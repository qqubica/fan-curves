[package]
name = "fan-core"
description = "fan-curves engine: staircase curves + the MacBook-feel response filter"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
